1、对象持久化之前必须序列化
2、持久化的对象是二进制序列
3、什么是对象的持久化？（将对象保存到数据库或者硬盘上的操作就是持久化）
4、什么是对象的序列化？（将对象转化为二进制序列，以便持久化、在网络中传输、在进程间通讯）
5、没有对象序列化技术之前，怎么实现的对象持久化。
6、对象持久化技术，需要对象序列化技术的支持

7、序列化接口Parcelable不支持对象的持久化的原因是什么？
8、Android数据缓存，数据缓存包括对象的持久化；但是对象的持久不只有数据缓存。

9、查这些主要是为了数据缓存做铺垫，

想 使用Parcelable作为对象的序列化接口，但是不支持对象的持久化。转为json进行存储？

greenDao对象关系映射工具与Parcelable能否结合使用