Android中，在java代码中new的控件是没有有效ID的（默认的ID都为-1，如果设置多个控件的点击事件的时候会响应异常），这个时候就需要我们在new出空间的时候通过generateViewId()方法给控件设置一个ID，该方法是定义在View类中的。
但是generate方法是在API17加入的，对于之前的API版本，我们就需要自己解决，但是可以通过查看源码，


getIdentifier（）：方法是为什么不可以？
